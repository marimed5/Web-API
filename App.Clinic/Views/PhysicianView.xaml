<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App.Clinic.Views.PhysicianView"
             Title="PhysicianView"
             NavigatedTo="PhysicianView_NavigatedTo"
             >
    <VerticalStackLayout>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="10*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label Grid.Column="0" Grid.Row="0" Text="Name:"/>
            <Entry Grid.Column="1" Grid.Row="0" Text="{Binding Name}"/>
            <Label Grid.Column="0" Grid.Row="1" Text="License Number:"/>
            <Entry Grid.Column="1" Grid.Row="1" Text="{Binding LicenseNum}"/>
            <Label Grid.Column="0" Grid.Row="2" Text="Specializations:"/>
            <Entry Grid.Column="1" Grid.Row="2" Text="{Binding Specializations}"/>
            <Label Grid.Column="0" Grid.Row="3" Text="Graduation Date:"/>
            <Entry Grid.Column="1" Grid.Row="3" Text="{Binding GradDate}"/>
        </Grid>
        <Button
            Text="Ok"
            Clicked="AddClicked"
            />
        <Button
            Text="Cancel"
            Clicked="CancelClicked"
            />
    </VerticalStackLayout>
</ContentPage>